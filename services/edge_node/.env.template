# Edge Node Configuration Template (Auth0 M2M only)
# Copy to .env and fill with real values. Auth0 M2M credentials are REQUIRED - there is no API key fallback.

# Node Identity
DC_ID=EDGE_NODE_01

# Server
PORT=8000

# MQTT Configuration - point to your aggregator (or central MQTT broker)
MQTT_ENABLED=true
MQTT_BROKER_URL=mqtt://YOUR_AGGREGATOR_IP:1883
MQTT_USERNAME=
MQTT_PASSWORD=

# HTTP Aggregator - point to your aggregator API
AGGREGATOR_URL=http://YOUR_AGGREGATOR_IP:3000

# Auth0 MACHINE-TO-MACHINE (REQUIRED)
# Create an M2M application in Auth0, grant it access to the aggregator API (audience),
# then set the following values here.
AUTH0_DOMAIN=dev-o237ddonm6b5vfn2.us.auth0.com
AUTH0_AUDIENCE=https://vpp-aggregation-platform.com/api
AUTH0_CLIENT_ID=your_m2m_client_id
AUTH0_CLIENT_SECRET=your_m2m_client_secret

# Telemetry interval (seconds)
TELEMETRY_INTERVAL=5

# Notes:
# - The edge node will fail to start if Auth0 M2M variables are missing or invalid.
# - There is no API key fallback — use Auth0 M2M credentials only.
